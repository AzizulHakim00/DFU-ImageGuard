# DFU-PolarMorphNet V2

This directory contains the single-file implementation `DFU_PolarMorphNet_AllInOne.py` after verified CI assembly. Use the Colab notebooks under `notebooks/` and the research protocol under `docs/DFU_POLARMORPHNET_PROTOCOL.md`.

The project performs a new V2 training run; V1 checkpoints cannot represent the new architecture. It does not contain fabricated results.
