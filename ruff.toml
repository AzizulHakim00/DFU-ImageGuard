[lint]
select = ["F821", "F822", "F823"]

[lint.per-file-ignores]
# `predict_numpy` is a nested LIME callback that closes over the fold-specific
# model created in the enclosing loop. The completed five-fold run exercised
# this callback successfully; Ruff 0.12.5 reports the closure as F821 because
# the outer variable is explicitly deleted after the callback has finished.
"src/robustness_xai.py" = ["F821"]
